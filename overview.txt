////

Countdown for timers:


Classes:
	Timer Class:
		Time Left
		(Time start Time end Time current)
	User Class:
		List of all timers
	Display Class:
		shows the graphic of how long is left
	Log Class:
		Only function is to print whatever is happening in the other classes and add it to log.txt

		---> Make sure ever line has a log input in which it sends the current thing to log.txt